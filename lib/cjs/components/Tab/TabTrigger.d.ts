import React from 'react';
import type { TabsTriggerProps } from '../../model/tab';
export default function TabTrigger({ className, value, activeClass, children, ...rest }: TabsTriggerProps): React.JSX.Element;
